#include <stdio.h>

int main() {
    int num1, num2, sum;

    // 標準入力から2つの整数を取得
    printf("1つ目の整数を入力してください: ");
    scanf("%d", &num1);

    printf("2つ目の整数を入力してください: ");
    scanf("%d", &num2);

    // 2つの整数の合計を計算
    sum = num1 + num2;

    // 合計を出力
    printf("合計は %d です\n", sum);
    while(1){
        
    }
    return 0;
}
